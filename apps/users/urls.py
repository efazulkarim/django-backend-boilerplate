"""Users app URL configuration."""

from __future__ import annotations

from django.urls import URLPattern

urlpatterns: list[URLPattern] = [
    # Authentication URLs handled by django-allauth
    # No custom URLs needed for now
]
